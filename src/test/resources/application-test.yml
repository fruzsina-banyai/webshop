spring:
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
  datasource:
    url: jdbc:tc:postgresql:16.1:///webshop_test_db

  jpa:
    show-sql: true
  properties:
    hibernate:
      format_sql: true